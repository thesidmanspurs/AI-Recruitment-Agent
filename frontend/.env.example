# Client-safe env only. Vite inlines any VITE_* var into the browser bundle —
# NEVER put secrets here. Backend secrets live in the repo-root .env.

# Override only when the backend is hosted on a different origin from the SPA.
# Leave blank for same-origin (default dev: Express + Vite middleware on :3000).
VITE_API_BASE_URL=
